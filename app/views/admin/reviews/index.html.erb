<div class="page-header">
  <%= link_to new_admin_review_path, class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    New Review
  <% end %>
  <h1>Listing reviews</h1>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
            <th>Product</th>
            <th>User</th>
            <th>Comment</th>
            <th>Rating</th>
            <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @reviews) do |review| %>
            <td><%= review.product_id %></td>
            <td><%= review.user.try(:email) %></td>
            <td><%= review.comment %></td>
            <td><%= review.rating %></td>
            <td><%= link_to 'Show', [:admin, review] %></td>
        <td><%= link_to 'Edit', edit_admin_review_path(review) %></td>
        <td><%= link_to 'Destroy', [:admin, review], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      <% end %>
    </tbody>
  </table>
</div>
